import { EmmetConfiguration } from 'vscode-emmet-helper';
import { CompletionList, Hover, Position, SymbolInformation } from 'vscode-languageserver';
import { DocumentManager, Document } from '../../lib/documents';
import { LSConfigManager } from '../../ls-config';
import { HoverProvider, CompletionsProvider } from '../interfaces';
export declare class HTMLPlugin implements HoverProvider, CompletionsProvider {
    private emmetConfig?;
    private configManager;
    private lang;
    private documents;
    private styleScriptTemplate;
    constructor(docManager: DocumentManager, configManager: LSConfigManager, emmetConfig?: EmmetConfiguration | undefined);
    doHover(document: Document, position: Position): Hover | null;
    getCompletions(document: Document, position: Position): CompletionList | null;
    private isInComponentTag;
    private getLangCompletions;
    doTagComplete(document: Document, position: Position): string | null;
    private isInsideMoustacheTag;
    getDocumentSymbols(document: Document): SymbolInformation[];
    private featureEnabled;
}
